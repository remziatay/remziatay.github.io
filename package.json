{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Portfolio of Remzi Atay",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html --open --out-dir dist-dev",
    "build": "rimraf dist && parcel build index.html --no-source-maps",
	"predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "jquery": "^3.5.1"
  }
}
